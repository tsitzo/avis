import { StyleSheet, Text, View } from "react-native";
import React from "react";

const NewsTileBookmarkButton = () => {
  return (
    <View>
      <Text>NewsTileBookmarkButton</Text>
    </View>
  );
};

export default NewsTileBookmarkButton;

const styles = StyleSheet.create({});
